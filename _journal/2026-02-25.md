# 2026-02-25

## Session 1: cachyos Stack Deployment + Mesh Heartbeat Unification

### What changed

Replicated the full markweb stack from mmc onto cachyos (7 services), made mesh heartbeats config-driven via env vars, scrubbed sensitive data from git history, and added security guardrails to CLAUDE.md.

See `~/.gh/_journal/2026-02-25.md` sections 1-4 for full details (cross-project journal).

**2 commits:** `0cc8369`, `d986204`

1. **`0cc8369` — Make mesh heartbeat config-driven via MESH_NODE_NAME/MESH_NODE_WG_IP env vars**
   - New `config/mesh.php` reads from env
   - `routes/console.php` heartbeat reads config instead of hardcoded values
   - Deployed to all 3 nodes with per-node env vars

2. **`d986204` — Add public repo security rules, update journal with mmc migration and mesh setup**
   - Security section added to top of CLAUDE.md: never commit public IPs, API keys, secrets
   - Scrubbed public IPs from `_journal/2026-02-24.md` (git history rewritten)

## Session 2: Standardise on `web.*` C&C URLs + HTTP Basicauth Gate

### What changed

Established `web.*` as the canonical markweb C&C URL convention across all 3 nodes, replacing the previous `app.motd.com` / `markweb.kanary.org` / `markweb.goldcoast.org` names. Added Caddy-level HTTP basic auth as a front gate before the Laravel login page.

**1 commit:** `03a060e`

1. **`03a060e` — Replace default Laravel README with markweb project overview**
   - Covers architecture, mesh deployment, stack, `web.*` convention, origin story

### Infrastructure changes (not in git)

#### HTTP basicauth gate (all 3 nodes)

Caddy `basicauth` directive with bcrypt hash, scoped via `@protected` matcher to exclude:
- `/api/*` — API endpoints
- `/app/*` — Reverb WebSocket
- `/broadcasting/*` — broadcast auth
- `/jmap/*` — Stalwart JMAP proxy
- `/.well-known/*` — ACME, JMAP discovery
- `/robots.txt` — crawler access

Credentials: `markc` / `markweb_gate_2026`

#### mmc — web.motd.com

- Reissued motd.com cert with `web.motd.com` SAN added (used `acme.sh --standalone` — port 80 wasn't open)
- New cert SANs: motd.com, www.motd.com, app.motd.com, web.motd.com, autoconfig.motd.com, autodiscover.motd.com
- Caddyfile: `web.motd.com` with basicauth, `app.motd.com` → 301 redirect to `web.motd.com`
- `.env`: `APP_URL=https://web.motd.com`, `REVERB_HOST=web.motd.com`

#### mko — web.kanary.org

- Caddyfile: `(markweb-gate)` shared snippet, site block renamed to `web.kanary.org` only (removed `markweb.kanary.org` alias)
- `.env`: `APP_URL=https://web.kanary.org`, `REVERB_HOST=web.kanary.org`

#### cachyos — web.goldcoast.org

- Caddyfile: site block renamed to `web.goldcoast.org` only (removed `markweb.goldcoast.org` alias)
- `/etc/hosts`: removed `markweb.goldcoast.org` (only `web.goldcoast.org` remains)
- `.env`: `APP_URL=https://web.goldcoast.org`, `REVERB_HOST=web.goldcoast.org`

### Verification

| Node | URL | Without auth | With auth | Ungated paths |
|------|-----|-------------|-----------|---------------|
| mmc | `web.motd.com` | 401 | 200 | 200 |
| mko | `web.kanary.org` | 401 | 200 | 200 |
| cachyos | `web.goldcoast.org` | 401 | 200 | 200 |

`app.motd.com` returns 301 → `web.motd.com`.

### Gotchas discovered

- **acme.sh standalone mode** — when port 80 has no listener (FrankenPHP only on :443 with `auto_https off`), use `--standalone` instead of `-w /var/www/html`. acme.sh starts its own temp HTTP server for the challenge.
- **FrankenPHP reload not supported** — `Type=simple` doesn't support `systemctl reload`; must use `restart`.
- **mmc git push needs SSH** — HTTPS remote can't push (no device for credential input). SSH remote needs key + known_hosts setup. Simpler to commit locally and push from cachyos.
