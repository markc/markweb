# 2026-02-27

## Session 1: gc Container Bootstrap + 3-Node Deploy

### What changed

Brought the `gc` Incus container (Debian 13) up to full mesh-ready status as a security-isolated markweb node, replacing the direct cachyos host exposure. Added gc as a deploy target and verified full 3-node deployment.

### gc container audit

Found the container was mostly provisioned — FrankenPHP 1.11.2, PHP 8.4, PostgreSQL 17 (27 migrations ran), Stalwart 0.15.5, Valkey, PowerDNS, SSL certs, frontend built. Missing: SSH, Ollama, bun, WireGuard forwarding, git repo tracking.

### DNS fix inside gc

Unbound's catch-all forward zone pointed to `10.11.0.1` (Incus bridge gateway) but no DNS listener existed there — host runs `systemd-resolved` on `127.0.0.53` only. Fixed by changing the catch-all forwarders to `1.1.1.1` / `8.8.8.8` in `/etc/unbound/unbound.conf.d/gc-resolver.conf`. Mesh zone forwarding (kanary.org, motd.com, goldcoast.org) to local PDNS was already correct.

### SSH setup

Installed `openssh-server`, enabled the service. Generated `ed25519` keypair on cachyos host (`~/.ssh/id_ed25519`), added pubkey to both `root` and `u1002` authorized_keys inside gc. Verified: `ssh root@10.11.0.2` and `ssh u1002@10.11.0.2` both work.

### Ollama inside gc (self-contained)

Installed ollama + `nomic-embed-text` model inside gc for self-contained embeddings. Updated `.env` to point `OLLAMA_URL` and `OLLAMA_HOST` to `127.0.0.1:11434` instead of the host (`10.11.0.1`). This makes gc a complete standalone node for resource benchmarking.

Cachyos host ollama retains `phi4` (14B params, 9.1GB) for local LLM testing, separate from the mesh embedding model.

### WireGuard DNAT forwarding

gc doesn't run WireGuard — the cachyos host terminates WG and forwards mesh traffic into the container via iptables DNAT. Added to `/etc/wireguard/wg0.conf` PostUp/PostDown:

- **PREROUTING DNAT:** traffic on wg0 for `172.16.1.4` ports 22,25,443,465,993,8443 → `10.11.0.2`
- **FORWARD ACCEPT:** wg0 → incusbr0 for those ports
- **POSTROUTING MASQUERADE:** incusbr0 → gc

Verified from mko: `curl -sfk https://172.16.1.4/` returns 401 (basicauth working). SSH DNAT also verified (connection reaches gc, auth rejection as expected since mko has no key for gc).

**Gotcha:** Traffic from cachyos itself to `172.16.1.4` doesn't hit PREROUTING (it's a local address routed via `lo`). Access gc directly via `10.11.0.2` from the host.

### Git repo initialisation in gc

App was deployed via copy, not git clone. Initialised git repo in place: `git init`, added HTTPS remote (no deploy keys per security policy), fetched origin/main, set upstream tracking. Clean working tree confirmed.

### Deploy script update

Added gc as a third deploy target in `bin/deploy`:
- New `NODE_SSH` associative array decouples node names from SSH hosts (gc uses `10.11.0.2`)
- Artisan cache commands now run as vhost user (`sudo -u $user`) instead of root
- `bin/deploy gc`, `bin/deploy all` both work

### Node fixes during deploy

- **mko:** SSH key at `/srv/web.kanary.org/.ssh/id_ed25519` had 0640 perms → fixed to 0600. bun binary not executable → `chmod +x`. node_modules vite.js not executable → `bun install` to fix.
- **mmc:** `/srv/web.motd.com` owned by root → `chown u1002:u1002`. bun not installed → installed. Same vite permissions issue → `bun install` to fix.

### Final 3-node deploy verified

```
==> [mko] built in 13.37s ✓
==> [mmc] built in 11.52s ✓
==> [gc]  built in 10.15s ✓
==> Deploy complete
```

### Commit

- `c12e12a` — Add gc (local Incus CT) as deploy target

## Session 2: Migrate Mesh to Isolated 172.16.2.0/24 WireGuard Network

### What changed

Migrated the entire markweb 3-node mesh from shared 172.16.1.0/24 network (mixed with customer hosts) to a dedicated, isolated 172.16.2.0/24 WireGuard network. gc container now has its own WG interface (no more host DNAT), and cachyos host has a separate admin-only WG identity.

### New network topology

| Node | Old IP | New IP | WG port |
|------|--------|--------|---------|
| gw wg1 | — | 172.16.2.1 | 51822 |
| mko | 172.16.1.210 | 172.16.2.210 | via gw 51822 |
| mmc | 172.16.1.9 | 172.16.2.9 | via gw 51822 |
| gc | 172.16.1.4 (shared) | 172.16.2.4 (own key) | via gw 51822 |
| cachyos | 172.16.1.4 (shared) | 172.16.2.5 (own key) | via gw 51822 |

### Execution order

1. Generated new keypairs for gc and cachyos (+ gw wg1)
2. gw router: added wg1 interface (port 51822) + firewall zone (no wg0↔wg1 forwarding)
3. mko: updated wg0.conf to 172.16.2.210, peer = gw wg1
4. cachyos: new keypair, 172.16.2.5, removed all DNAT/FORWARD/MASQUERADE rules
5. gc: new keypair, own wg0 inside container at 172.16.2.4, enabled on boot
6. mmc: updated to 172.16.2.9 via gw public IP:51822

### DNS updates

- Updated all A records in kanary.org, motd.com, goldcoast.org from 172.16.1.x to 172.16.2.x
- Deleted old reverse zone (1.16.172.in-addr.arpa), created new (2.16.172.in-addr.arpa) as Master
- Updated PDNS configs on all 3 nodes (listen addresses, allow-notify, allow-axfr, masters)
- Fixed gc and mmc secondary databases (supermasters + domains master IP)

### Gotchas

- **`pdnsutil replace-rrset` double-qualifies names** — `replace-rrset kanary.org web.kanary.org A 300 ...` creates `web.kanary.org.kanary.org` instead of updating `web.kanary.org`. Fixed by updating records directly in PostgreSQL.
- **wg-quick systemd state** — after manual `wg-quick down/up`, systemd thinks the service is still active from the old invocation. Need `systemctl reset-failed` before `systemctl start`.
- **PDNS local-address binding** — mko PDNS was bound to 172.16.1.210, not 0.0.0.0. Had to update the drop-in config and restart.
- **Zone transfer serials** — secondaries had same serial as primary after replace-rrset (which didn't actually update). Had to bump serials after fixing records.

### Cleanup

- Removed unbound from gc (systemd-resolved handles split-horizon via wg0 PostUp)
- Updated .env MESH_NODE_WG_IP on all 4 nodes
- Updated README.md, docs/ai.txt, memory files with new IPs
